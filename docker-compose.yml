version: "3.9"

services:
  healthcare-ai:
    build: .
    container_name: healthcare-ai
    ports:
      - "8000:8000"

    volumes:
      - ./vector_store:/app/vector_store
      - ./data:/app/data

    env_file:
      - .env

    extra_hosts:
      - "host.docker.internal:host-gateway"
